# MarketPulse 工作总结 2026-05-29 ~ 05-30

## 一、今日代码改动

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| 1 | `apps/api/routes/sse_bars.py` | 299 个死人 consumer 抢 Redis Stream 消息，导致 SSE 断流 | `xreadgroup`（work queue）→ `xread`（pub-sub），零僵尸 |
| 2 | `apps/web/lib/use_kline_stream.ts` | 切周期时旧 SSE 数据残留，闪一根 K 线 | interval/symbol 变化时先 `setBars([])` |
| 3 | `apps/web/components/KLineChart.tsx` | 每次 SSE tick 全量 `setData`（500 根），滑动卡顿 | 增量 `series.update()` O(1)，去 `livePrice` 冗余 deps |
| 4 | `apps/web/app/page.tsx` + `globals.css` | 自选列表无价格变动反馈 | 涨绿跌红 0.9s CSS animation，`useRef` 记历史价 |
| 5 | `apps/web/app/page.tsx` | Dashboard 缺周线月线 tab | interval tabs 加 `1wk` `1mo` |
| 6 | `apps/api/routes/cd_signals.py` + `apps/web/lib/cd_signals_api.ts` + `apps/web/app/page.tsx` + `apps/web/app/notifications/page.tsx` | Crypto 信号被全局 top 20 淹没 | API 加 `market` 参数后端过滤（拉大窗→infer_market→截断），notifications 页读 query param |
| 7 | `apps/web/app/page.tsx` | 切换市场图表标的不跟随 | `useEffect` 监听 market→重置 selected，`marketSelectedRef` 记住每个市场用户选择 |

### 上会话（05-29 晚间）主要改动

**UI 整体重构**：从旧 Tailwind 风格迁移到 Design B Enhanced（富途/Moomoo 风格）

| 文件 | 改动 |
|---|---|
| `apps/web/app/globals.css` | 完整 Design System：CSS 变量 light/dark 主题、sidebar/topbar/panel/data-table/int-tab/sig-badge 组件 class |
| `apps/web/app/layout.tsx` | 新布局：左侧 sidebar 导航 + TopBar（4 市场 tab 切换 + ClientTime 组件） |
| `apps/web/lib/market-context.tsx` | **新建**：全局市场状态 React Context（MarketProvider / useMarket） |
| `apps/web/app/page.tsx` | Dashboard 完整重写：指数卡片 + 自选列表（batch SSE 实时价）+ K 线预览（滑动翻页+缩放）+ 信号面板 |
| `apps/web/app/symbol/[code]/page.tsx` | 详情页：统一数据管道（REST 分页 + SSE 实时）、poll:false、全周期 tab |
| `apps/web/app/market/page.tsx` | 市场总览页 |
| `apps/web/app/watchlist/page.tsx` | 自选管理页 |
| `apps/web/app/notifications/page.tsx` | 信号通知页 |
| `apps/web/app/ai-market/page.tsx` | AI 大盘页 |
| `apps/web/components/KLineChart.tsx` | 滑动翻页（`subscribeVisibleLogicalRangeChange`）+ 删除 StaleBadge + 屏幕自适应 |
| `apps/web/components/StaleBadge.tsx` | **删除**（回填完成后不再需要） |
| `apps/web/components/IndexCard.tsx` | 数据源切换到统一 bars 接口 |

**数据底盘建设**

| 文件 | 改动 |
|---|---|
| `scratch/init_data.py` | **新建**：三市场全量回填脚本。拉取 940 标的 × 8 周期全历史（~1350 万 bar）。含 `_GAP_TOLERANCE`、`_find_gap_start` 断点检测、`_pre_aggregate` 60m/4h/1wk/1mo 预聚合、大盘指数自动补入 |
| `apps/collector/jobs/aggregate_derived.py` | **新建**：`sweep_derived` 增量聚合 job。30min 间隔扫描 DuckDB，仅处理有新增源数据的标的（5m→60m/4h，1d→1wk/1mo） |
| `apps/collector/crypto/ws_consumer.py` | 1m 剔除（不入库）、NO_PROXY 移除（用户代理处理 WS）、8 周期全量 |
| `apps/collector/crypto/backfill.py` | 断点检测改为相邻 bar 连续性扫描 |
| `apps/collector/crypto/main.py` | 加 `attach_crypto_signal_jobs`（24×7 CD 信号扫描） |
| `apps/collector/ashare/bar_poller.py` | **新建**：A 股 SSE 订阅驱动按需轮询（10s），仅轮询有活跃 SSE 连接 + 8 个默认指数的标的 |
| `apps/collector/us/ws_consumer.py` | **新建**：美股 Alpaca WS 1m bar 消费（三写：DuckDB + Redis tail + SSE bus） |
| `apps/api/routes/sse_bars.py` | 加批量 `/api/sse/bars/batch` 端点（1 连接推送 N 标的实时价），路由顺序修复（batch 在参数化路由前） |
| `core/scheduler/scheduler.py` | 加 `attach_crypto_signal_jobs`（24×7 5 个 cron） |
| `core/cache/keys.py` | 加 `cache_bars_current`、`BUS_BARS_UPDATED` |

---


## 二、三市场 Collector 采集/存储/聚合/恢复 全景

### 架构总览

```
collector ×3 (独立进程, 各内嵌 FastAPI)
├─ crypto (port 8790): bars_crypto.duckdb
├─ us     (port 8789): bars_us.duckdb
└─ ashare (port 8788): bars_ashare.duckdb

共享: Redis (cache + Streams bus + state lock) + SQLite (state.db)
```

DuckDB 表结构：`bars(market, symbol, ts, interval, open, high, low, close, volume, amount?, turnover?, outstanding_share?)`，PRIMARY KEY `(market, symbol, interval, ts)`。

写入统一走 `BarRepo.insert_bars` → DataFrame 批量 `INSERT ... ON CONFLICT DO UPDATE`。

---

### Crypto（Binance）

**采集**：Binance 组合 WS 单连接 40 路 stream（5 标的 × 8 周期），`k.x=true` 为收盘 bar，`k.x=false` 为进行中 bar。断线指数退避重连（1s→60s）。

**存储到 DuckDB 的周期**：全部 8 个 —— **5m / 15m / 30m / 60m / 4h / 1d / 1wk / 1mo** —— Binance 原生提供，无需聚合。

**Redis 写入**：

| 数据类型 | WS final bar | WS in-progress bar | Backfill |
|---|---|---|---|
| DuckDB insert | ✅ | ❌ | ✅ |
| Redis tail (`cache:bars:*:tail`) | ✅ | ❌ | ✅ |
| Redis current (`cache:bars:*:current`) | ❌ | ✅ (TTL 2×interval) | ❌ |
| SSE bus (`bus:bars.updated`) | ✅ (final=true) | ✅ (final=false) | ❌ |

**聚合**：无。

**崩溃恢复**：启动时 `run_backfill()` 串行对 40 个组合做 gap 检测——查 DuckDB 最近 7 天 bar，反向扫描相邻 bar 间隔，对照 `_GAP_TOLERANCE`（5m:10min, 15m:30min, 30m:1h, 60m:2h, 4h:8h, 1d:2d, 1wk:10d, 1mo:35d）。找到断点 → Binance REST 补全到此刻。无 bar → 从 2017-07-01 拉全历史。每日 04:00 UTC Cron 兜底。

---

### 美股（Alpaca + yfinance）

**采集**：
- Alpaca WS（IEX free tier）→ **仅 1m** bar → DuckDB + Redis tail + `bus:bars.updated`
- `fetch_intraday` cron（交易时段每 15min）→ Alpaca REST 拉 **5m/15m/30m** 原生 bar
- `tick_snapshot`（10s）→ 实时报价 → QuoteCache → `flush`（60s）→ 1m DuckDB
- 1d 走 Alpaca REST / yfinance fallback

**存储到 DuckDB 的周期**：

| 周期 | 来源 | 备注 |
|---|---|---|
| 1m | Alpaca WS + QuoteCache flush | 原生 |
| 5m | Alpaca REST cron | 原生 |
| 15m | Alpaca REST cron | 原生 |
| 30m | Alpaca REST cron | 原生 |
| 60m | **聚合自 5m**（`sweep_derived` 30min） | session 对齐切桶（ET 04:00-20:00） |
| 4h | **聚合自 5m** | session 对齐切桶 |
| 1d | Alpaca REST / yfinance | 原生 |
| 1wk | **重采样自 1d**（pandas W-FRI） | `sweep_derived` |
| 1mo | **重采样自 1d**（pandas ME） | `sweep_derived` |

**聚合逻辑**（`core/services/intraday_aggregator.py`）：5m 原始 bar（close 语义）→ 按 `market_sessions.SESSIONS["us"]` 切桶 → OHLCV 聚合。60m 桶遇 lunch break/收盘裁剪为半桶。1wk 用 pandas `resample("W-FRI")`，1mo 用 `resample("ME")`。

**Redis 写入**：

| 数据类型 | WS consumer | REST cron | sweep_derived |
|---|---|---|---|
| DuckDB insert | ✅ (1m) | ✅ | ✅ |
| Redis tail | ✅ | ✅ | ✅ |
| SSE bus (`bus:bars.updated`) | ✅ (仅 1m) | ❌ | ❌ |

> ⚠️ **美股 SSE 实时推送当前仅覆盖 1m**。5m/15m/30m/1d 由 REST cron 写入 DuckDB 但不发 SSE 总线。后续若需 5m+ 实时推送，需在 REST cron 或 sweep_derived 中加 `xadd`。

**崩溃恢复**：无启动回填。依赖 `sweep_derived`（30min）补聚合缺口、`fetch_intraday` cron（15min）补 REST 缺口、`refill_consumer` 处理 API 触发的按需补、WS 自动重连补 1m。

---

### A 股（sina/em/ths 经 AKShare）

**采集**：**无 WebSocket**，全部 REST 轮询。
- `BarPoller`（10s）→ SSE 订阅驱动，仅轮询有活跃 SSE 连接 + 8 个默认指数的标的 → 发 `bus:bars.updated`
- `fetch_intraday` cron（交易时段 15min）→ 5m 原生 bar
- `tick_snapshot`（10s）→ sina 实时报价 → QuoteCache → `flush`（60s）→ 1m DuckDB

**sina 非交易时段防污染**：ashare adapter 过滤掉 BJT 09:15-11:30 和 13:00-15:00 之外的 bar（sina 在休市时段用昨日收盘价填充假 bar）。

**存储到 DuckDB 的周期**：

| 周期 | 来源 | 备注 |
|---|---|---|
| 1m/5m/15m/30m | sina REST（ak_call） | 原生 |
| 60m | **聚合自 5m**（`sweep_derived` 30min） | BJT 上午 09:30-11:30 / 下午 13:00-15:00 session 切桶 |
| 4h | **聚合自 5m** | session 对齐切桶 |
| 1d | sina/em daily REST | 原生 |
| 1wk | **重采样自 1d** | `sweep_derived` |
| 1mo | **重采样自 1d** | `sweep_derived` |

**Redis 写入**：

| 数据类型 | BarPoller | REST cron | sweep_derived |
|---|---|---|---|
| DuckDB insert | ✅ | ✅ | ✅ |
| Redis tail | ❌ | ✅ | ✅ |
| SSE bus (`bus:bars.updated`) | ✅（仅最新一根, final=true） | ❌ | ❌ |

> ⚠️ **A 股 SSE 实时推送当前仅覆盖有活跃 SSE 订阅的标的**。BarPoller 通过 Redis `state:subscribe:*` key（TTL 120s，由 SSE 连接心跳续期）决定轮询哪些标的。8 个默认指数始终轮询。无订阅的普通标的无实时推送。

**崩溃恢复**：同美股，无启动回填。`sweep_derived` + `fetch_intraday` cron + `refill_consumer` + BarPoller 启动后扫描 Redis 自动恢复。

---

### 三市场差异对比

| | Crypto | 美股 | A 股 |
|---|---|---|---|
| 实时数据源 | Binance WS（8 周期） | Alpaca WS（仅 1m） | sina REST 10s 轮询 |
| 原生周期数 | 8 个全原生 | 1m/5m/15m/30m/1d | 1m/5m/15m/30m/1d |
| 聚合周期 | 无 | 60m/4h(5m→) + 1wk/1mo(1d→) | 同美股 |
| 启动回填 | ✅ gap 检测全量补 | ❌ cron + sweep 渐进补 | ❌ cron + sweep 渐进补 |
| SSE 总线写者 | WS（全 8 周期） | WS（仅 1m） | BarPoller（订阅标的） |
| 进程崩溃 SSE 恢复 | WS 重连即恢复 | WS 重连→仅 1m 恢复 | subscribe key 过期前(120s)自动恢复 |
| Leader 选举 | 无 | ✅ 每市场独立锁 | ✅ 每市场独立锁 |

---

## 四、所有主动 Refresh 场景

### 前端 REST 轮询（useSWR refreshInterval）

| 页面/组件 | 间隔 | 数据 |
|---|---|---|
| Watchlist 页 quote | **15s** | `/api/symbols/{s}/quote` |
| IndexCard（A 股） | 30s | `/api/indices/{s}/minute?days=1` |
| TopMoversCard | 30s | `/api/markets/{m}/top` |
| CDSignalPanel | 30s | `/api/cd-signals/by-symbol/{s}` |
| WatchlistSignalsPanel | 30s | `/api/cd-signals/watchlist-events` |
| Market 页 health | 30s | `/api/health` |
| Dashboard 指数卡片 | 60s | 1d bars history |
| Dashboard 自选价格 | 60s | 5m bars history（REST 打底，SSE 覆盖） |
| Dashboard 信号 | 60s | `/api/cd-signals?market=` |
| 详情页信号 | 60s | `/api/cd-signals/by-symbol/` |
| Notifications 页 | 60s | `/api/cd-signals?market=` |
| FundFlowPanel | 60s | fund_flow（仅 A 股开市） |
| VolumeIndicatorsPanel | 60s | volume_indicators |
| AI 大盘相关 ×4 | 60s | market-packet |
| IndexCard（港股） | 5min | `/api/indices/{s}/minute?days=30` |
| useBarsHistory（poll=true 时） | 60s | bars history |

> Dashboard + 详情页 K 线图表已 `poll: false`，由 SSE 推送替代 REST 轮询。

### 后端 Cron Jobs（APScheduler CronTrigger）

**CD 信号扫描**：
- A 股 10 个 job：交易时段对齐 BJT 上午/下午收盘（10:30/11:30/14:00/15:00 的 15m/30m/60m/4h/1d）
- 美股 10 个 job：对齐 ET pre-market(04:00-09:30) / core(09:30-16:00) / post-market(16:00-20:00)
- Crypto 5 个 job：24×7，15m 每 15min，30m 每 30min，60m 每小时，4h 每 4h，1d 每日 00:05 UTC

**数据采集**：
- `fetch:ashare:5m` / `fetch:us:5m`：交易时段每 15min
- `crypto:backfill_daily`：每日 04:00 UTC
- `chip:preload`：BJT 15:35
- `baseline_persist`：A 股/US/港股收盘后

### 后端 Interval 定时器（APScheduler IntervalTrigger）

| Job | 间隔 | 市场 |
|---|---|---|
| `tick` 快照 | **10s** | 全市场 |
| `index_minute` | 30s / 60s | A 股 / 美股 |
| `flush` 持久化 | 60s | 全市场 |
| `market_dashboard` / `market_top` / `ai_packet` | 60s | A 股 / 港股 |
| `ff:north` | 2min | A 股 |
| `ff:symbols` | 30min | A 股 |
| `sweep_derived` | 30min | A 股 / 美股 |

### 后端常驻循环

| 循环 | 机制 |
|---|---|
| BarPoller（A 股） | 10s REST 轮询 sina，SSE 订阅驱动启停 |
| Crypto WS consumer | Binance 组合 WS 长连（40 stream），断线指数退避重连 |
| US WS consumer | Alpaca WS 长连（1m bar），断线指数退避重连 |
| Refill consumer | 阻塞读 Redis Stream `bus:bars.refill_request`，按需补 bar |
| Leader 续期 | 每 5s Redis SETNX 续 leader 锁 |

### 不需要 Refresh 的数据（纯 SSE 推送）

| 数据 | 推送路径 |
|---|---|
| Crypto K 线实时尾部（5m-1mo） | Binance WS → `bus:bars.updated` → SSE `/api/sse/bars/{s}/{iv}` |
| 自选列表实时价（crypto 目前） | 同上 + 批量 `/api/sse/bars/batch` |
| CD 信号扫描结果 | Cron job → SQLite → REST API（60s SWR 刷新） |
